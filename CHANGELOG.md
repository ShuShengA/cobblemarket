# 更新日志 / Changelog

## 1.1.1（开发中，未发布）

### 修复

- **英文模式下部分界面文字溢出**（关闭「强制使用 Unicode 字体」后，个别英文文字会画出按钮或弹窗边框；中文界面不受影响）。此前未发现是因为开发环境默认开启了该字体选项，目前已发现的问题均已修复
- **中文搜索搜不全精灵**：精灵市场与管理面板的精灵搜索框输入中文时，如果这个词同时出现在多个精灵名字里（搜「鬼斯」——还有「鬼斯通」；搜「火」——小火龙 / 火恐龙 / 喷火龙），只会出现其中一个，另一个怎么搜都搜不出来（出现哪个取决于内部顺序，看起来像随机的）；现在**名字里含这个词的都会出现**。英文与拼音搜索不受影响
- **黑名单 / 价格限制 / 求购单里输入中文精灵名会配错精灵**：这三个界面是按名字指定一只精灵的，此前输入「鬼斯」实际选中的是「鬼斯通」（同一个原因）——配黑名单、限价规则或发求购单时悄悄配到了别的精灵上；现在改成**名字完全相同的优先**，并且匹配到多个时预览图旁边会出现**左右箭头**可以逐个切换（带「第几个 / 共几个」计数），选中的预览图就是最终生效的那只
- **按 F1 隐藏 HUD 时余额 HUD 依然显示**：原版 HUD 全部隐藏后余额 HUD 还留在画面上（截图/录屏时挡画面），现在随 F1 一起隐藏
- **聊天里的拍卖播报悬停看不到证章**：精灵详情面板都有证章展示，唯独在聊天栏悬停拍卖播报时没有——同一只精灵两处看到的详情对不上。现已补上（位置与其他详情一致：亲密度下方，超过 3 个折成「+N」）

## 1.1.0（开发中，未发布）

### 新功能

#### 金融系统（喵喵银行）

- **喵喵银行信用借贷系统**：入口界面新增「喵喵银行」入口——信用借款按 3/6/12 期（每 7 天一期）偿还，可用额度按玩家历史交易额自动计算，借款即时到账；借贷资金由服务器准备金池统一记账
- **应急贷款（喵喵的帮助）**：喵喵银行内申请借款——实时显示可用额度与当前欠款，输入金额、选择分期方案（3/6/12 期 · 每期费率可配），确认后立即到账
- **还款柜台**：喵喵银行内新增还款入口——未结清借款列表（剩余本金/期数/逾期状态一目了然），可「还一期」或「提前结清」（剩余本金 + 实算利息一次付清）
- **到期自动划扣与逾期**：每期到期自动从玩家余额划扣当期还款（保留保底余额不扣空钱包）；余额不足进入逾期——禁止新增借款并收到红色通知，逾期利息按天持续累积，还清欠期后自动恢复
- **信用审计账本**：借贷与还款全量写入独立 CSV 账本（借款创建/逾期/结清事件 + 每笔还款的本金利息拆分，自动/主动/结清方式分明），中英双份按天分文件，供服主查账
- **喵喵银行配置界面**：金融系统全部参数（总开关/现金贷开关/分期方案与费率/额度计算权重/自动划扣保底余额/同 IP 欠款上限/逾期制裁三档天数）在独立的喵喵银行配置界面集中编辑（服务器配置界面底部入口进入）
- **逾期制裁三档**：借款到期未还将按逾期天数逐级升级——逾期 7 天市场手续费翻倍、逾期 14 天市场交易冻结（还清欠款自动解冻）、逾期 30 天注销为坏账（服主收到告警，玩家交易保持冻结，可手动解封）
- **喵喵支付（信用消费）**：购买精灵/物品时可选「喵喵支付」——选择分期方案（3/6/12 期 · 每期费率）后由服务器准备金直接垫付给卖家，买家按计划分期偿还；逾期或已注销的玩家不能使用
- **服主金融报表与坏账干预**：管理员面板新增准备金池/坏账总额行（准备金为负即服主负债时红色告警）；全服借贷流水显示借款人最近 IP（排查小号同 IP 借款）；坏账可撤销（全服流水界面「撤销」按钮或 `/market loan clear` 命令）——撤销后玩家恢复借款资格并自动解除冻结
- **全服累计成交额**：入口界面标题下方显示全服历史累计成交额（服务器经济规模氛围展示）
- **喵喵银行存款（活期）**：喵喵银行新增「存款/取款」——闲钱存进银行按日计息（日利率可配，利息从准备金池支出），随时可取（含累计利息）；存款充实准备金池形成存贷闭环，金融总开关关闭时取款不受影响
- **额度增长冷却**：成交后一段时间内（默认 24 小时，服主可配，0 = 关闭）不计入借款额度——额度增长延后到账，封堵「现刷现借」组团套现跑路的快进快出窗口；交易对检测的窗口天数与笔数上限也可配（默认 30 天/3 笔）
- **还款日前提醒**：每期到期前一天，玩家会收到黄色提醒（应还本金金额），避免忘记还款日直接逾期
- **喵喵银行规则面板**：喵喵银行界面新增「规则」按钮（返回按钮下方），悬停查看借款规则与后果（分期/自动划扣/逾期三档制裁/额度共享/利息），关键字红色标注
- **全部借款历史坏账页签**：OP 全服借贷流水新增「全部/坏账」页签，一键筛出所有坏账记录；已还清借款记录保留 90 天后自动清理（审计账本永久可查）
- **存贷利率护栏**：存款日利率配置受贷款分期方案自动约束——设置超过套利安全线的存款利率时自动钳制到安全值并记录日志，杜绝「借钱存银行吃利息」的刷钱循环；贷款方案费率调低时存款利率同步收紧；保存后配置界面底部黄色提示显示钳制结果（如免息方案将利率清零），避免误判按钮失灵

#### 金融系统 · 卡片凭证（喵·紫金卡/喵·黑金卡）

- **喵·紫金卡**：高额度凭证物品——持有者借款额度固定为服主配置值（默认 100 万），全服发放数量上限可配（默认 20 张）；额度绑定持有者状态而非物品（刷物品漏洞复制出的卡无效）；卡片丢弃立即消失，可在喵喵银行补发（申请/补发时背包已满会拒绝发放并提示清理背包，付费前预检）；持卡玩家借款不受同 IP 欠款上限限制（防小号限制只拦普通玩家）；服主用 `/market card` 命令发放/收回/查看
- **喵·黑金卡**：比喵·紫金卡高一级的额度凭证——额度默认 500 万、全服上限默认 5 张（均可配）；申请喵·黑金卡必须持有喵·紫金卡（硬条件），其余七项申请条件与喵·紫金卡同构可配；获得喵·黑金卡自动移除喵·紫金卡资格（升级替代，不占两个名额）；额度与手续费减免按喵·黑金卡计算；喵喵银行右侧喵·黑金卡图标进入申请界面；服主命令加卡种参数（`/market card give <玩家> black`、revoke/list 同理，缺省喵·紫金卡）
- **卡片管理界面（仅 OP）**：喵喵银行规则按钮下方新增「卡片管理」入口（按钮带喵·紫金卡/喵·黑金卡双卡微缩图标）——列表展示全部喵·紫金卡/喵·黑金卡持有者（名字+持有卡种图标），行尾「收回」按钮一键收回，与命令同效果
- **持有者展示面板**：喵喵银行两张卡下方各新增一个信息面板（所有玩家可见）——标题显示「当前持有人数/全服上限」，面板内列出持有者名单（玩家名+皮肤头像，行间分割线，可滚动）；发放/收回/申请成功后实时刷新
- **喵·紫金卡自行申请**：服主可开启玩家自行申请——喵喵银行左侧喵·紫金卡点击进入申请界面，逐条显示七项申请条件（资产/消费金额/额度/存款余额/图鉴遇见数/图鉴捕捉数/无逾期记录）与当前进度，全部满足即可申请；申请费用进准备金池；开启开关需 5 秒冷静期确认（防条件未配好就放开发卡）；两张卡的申请界面均显示卡的借款额度与手续费减免特权
- **喵·紫金卡持有者手续费减免**：服主可配置喵·紫金卡持有者的市场手续费减免比例（上架费/拍卖成交费/求购中介费全覆盖，与逾期翻倍叠加，默认关闭）——持有喵·紫金卡的玩家交易成本更低
- **补发喵·紫金卡凭证费**：补发喵·紫金卡凭证改为可配置收费（默认免费），费用进入准备金池；申请界面持有者视角直接显示补发费用
- **卡片申请净存款门槛**：喵·紫金卡/喵·黑金卡申请的「存款余额要求」改按净存款（活期存款 − 未还欠款）判定，借钱包装存款无法满足门槛；申请界面条件行显示净存款值
- **发卡动画与音效**：获得喵·紫金卡 / 喵·黑金卡时，卡片从申请界面飞出到屏幕中央并**播放专属音效**（两张卡各一枚，黑金卡的更悠长）；两种卡节奏不同——紫金卡飞 0.7 秒、停留 0.6 秒后淡出 0.6 秒，黑金卡飞 1 秒、停留 1.5 秒后淡出 0.7 秒，更慢更隆重

#### 其他

- **精灵亲密度显示**：所有精灵详情（市场悬停/拍卖详情/购买确认/求购交付/管理员面板的列表/上架预览/待领取）在六项个体值下方显示亲密度
- **物品搜索升级（照原版创造模式搜索语义并加强）**：物品搜索框（物品市场/管理员面板/黑名单/价格限制/拍卖/求购）支持按物品 ID、名称与完整 tooltip 文本搜索；Cobblemon 1.8 的招式学习器（TM）与附魔书支持**按招式名/附魔名搜索到具体变体**（原版创造模式搜不到 TM 招式，我们补齐了招式枚举）——搜「打鼾」在物品市场/拍卖直达打鼾的招式学习器，在黑名单/价格限制/求购单的添加对话框得到「招式学习器 · 打鼾」候选；附魔书按等级逐级展开（「附魔书 · 锋利 I」到「附魔书 · 锋利 V」，条目按等级及以上匹配——选 I 封禁全部锋利，选满级只封满级）；选中后条目只作用于该变体（不再连带封禁全部招式学习器）；以后 Cobblemon 新增招式自动沿用
- **黑名单/价格限制精确到物品变体**：添加对话框新增「从手持物品添加」（小手图标，选中变红再点「添加」确认，弹窗显示手持物品的图标与名称；开始搜索或点选物品会自动退出该模式）——手上拿什么就禁什么/限什么，如只禁「锋利V」的附魔书而不影响其它附魔书；条目按包含匹配（「锋利V」同时命中「锋利V+耐久III」，防止加个垃圾附魔绕过）；限价同物品多条规则按「最具体条目优先」生效（「锋利V+抢夺III」按自己的价格区间，不被「锋利V」条目压死）；搜索精确到变体（搜「打鼾」只出打鼾的条目）；批量解封按搜索结果精确解封（搜什么解封什么）
- **求购单物品组件要求**：发布物品求购单可点选手持图标附带手中物品的附魔等要求（如只收「锋利V」），交付物品不满足组件要求会被拒绝，求购单列表行同步显示组件要求
- **物品悬停词条全面升级**：所有物品列表与图标悬停改为真实物品词条——招式学习器的中文招式名、附魔书的中文附魔名与罗马数字等级等在列表行与悬停中完整可见；按住 Shift 展开完整词条、按住 Ctrl 显示组件明细（实时刷新）
- **精灵证章展示**：精灵详情面板（市场悬停/拍卖/拍卖出价弹窗/强制下架弹窗/管理员面板的拍卖/管理员面板的精灵列表/待领取/购买确认/求购单验收等）在亲密度下方新增证章区块——两条分割线内显示精灵拥有的全部证章图标（每行 10 个），证章纯外观展示不影响交易判定
- **精灵体型徽章**：所有精灵列表与详情（市场/拍卖/求购/管理员面板/上架预览/待领取/出售选择）显示体型徽章——XS/S/M/L/XL，阿尔法精灵显示 ALPHA；列表行排在携带物图标之后；聊天播报悬浮窗在性别后显示体型字母
- **余额 HUD 位置自定义**：市场入口界面设置里新增「余额 HUD 位置设置」——点「自定义」进入拖动模式，按住左键把余额 HUD 拖到屏幕任意位置，松手落位；靠近屏幕边缘或中心线会自动吸附并显示对齐辅助线；位置按屏幕比例保存，换分辨率或改 GUI 缩放都不会跑偏；设置里的标签「左上角显示市场余额」同步改名为「余额 HUD」
- **物品图标显示耐久**：市场、拍卖、求购、待领取与管理员面板的物品图标现在会显示耐久条（受损才显示，满耐久不显示）；悬停窗还会多一行「耐久：X / Y」，不用按 Shift 就能看到——避免花原价买到快报废的工具装备

### 改动

- 卡片申请的图鉴门槛：遇见数低于捕捉数时自动抬平到捕捉数（避免出现自相矛盾的配置）
- 部分按钮与标识去文字化
- 物品列表行物品名按稀有度着色（与物品栏悬浮一致）
- 拍卖场与管理员面板的拍卖场列表行价格改为缩写显示（如 1.2k / 3.5M，与物品市场、求购单一致；悬停与出价弹窗仍为完整千分位），行内不再标注「起拍」；卖家头像改为固定位置（各行对齐，与精灵市场一致），倒计时紧跟头像右侧，精灵名与体型徽章不再被挤压
- 拍卖场与管理员面板的拍卖场列表行的精灵名显示放宽：5 个字以内完整显示，超出才截断加省略号
- 选择要上架的精灵列表等级右移（与拍卖场选择精灵界面一致），精灵名再长、图标再多也不会盖住等级
- 精灵市场列表行的头像与等级右移、行内价格改为缩写显示（悬停窗仍为完整千分位），长名字配齐图标时不再盖住头像与等级
- 拍卖场与管理员面板的拍卖场的列表行头像与倒计时右移，体型徽章不再压住头像
- 管理员面板的精灵列表与待领取精灵列表的等级右移（与精灵市场一致），管理员面板的精灵列表行内价格改为缩写显示
- 求购单列表行的精灵名与物品名显示放宽：6 个汉字以内完整显示，不再被截成三个字
- 拍卖场、管理员面板的拍卖场、精灵市场、管理员面板的精灵列表与待领取精灵的行内头像、等级与倒计时再右移，与体型徽章留出间距；精灵市场、管理员面板的精灵列表与待领取的精灵名超过 6 个汉字时截断加省略号；拍卖场与管理员面板的拍卖场的行内出价次数紧贴价格、与出价按钮的间距收紧
- 拍卖上架界面的最低加价输入框提示改为显示服务器配置的默认数额（如「留空=100」），不再只写「留空=默认」
- 本项目许可证由 MIT 改为 GPL-3.0（1.0.1 及更早版本仍适用 MIT）
- **新增 Cobblemon Economy 兼容性警告**：Cobblemon 1.8 把图鉴的 `PokedexEntryProgress.CAUGHT` 改名成了 `OWNED`，而 Cobblemon Economy（截至 0.0.17）仍在引用旧字段名——**玩家获得、升级或进化精灵时（选初始精灵、捕捉、孵化、交易、升级、进化、形态变化）都会导致服务器崩溃**。启用了该货币的服务器启动时会收到醒目警告，建议改用 CobbleDollars / Impactor / 物品货币

### 修复

- **存款利息不满一天被清零**：利息此前按「距上次存取的整天数」结算，不满一天的部分会在下次存取时被直接丢弃——玩家存 23 小时 59 分与存 1 分钟收益相同（都是 0），每天存取一次的玩家永远拿不到利息；现改为按实际存入时长计息、不满一天按比例，日利率的含义不变
- **价格缩写顶出格子**：价格缩写（如 20 万显示为「200.0k」）在整数部分位数多时会超出网格/列表格子的边框——现在整数部分达到 10 以上就不再保留小数（「200k」），宽度与「5.0M」这类一致
- **喵喵银行背景比入口界面高 7px**：从入口进入喵喵银行时背景会整体上移一点；背景锚定公式的一项算错了（160 应为 146），现已与入口完全对齐
- **借款历史界面的「打开文件夹」改为 info 提示**：原先的按钮在服务器上只会打开客户端自己的目录、没有意义，现在改为左上角一个小图标，悬停显示借贷账本的存放路径；「全部 / 坏账」页签同步改为居中
- **拍卖场「我的」标签页**：该页和物品页一样没有筛选按钮行，列表现在紧接搜索框（原先按精灵页处理，多留了一行的空档），分割线随之紧贴列表；该页也不再显示上架按钮
- 修复对战期间队伍精灵交易的漏洞：对战中禁止上架/拍卖/交付精灵（原来抽走精灵会导致战斗内模型消失，已上架的精灵还能被切出继续战斗）；对战中购买/下架的精灵自动进待领取，战斗结束后领取
- 修复拍卖聊天播报的精灵悬浮详情中属性名未按属性色显示的问题
- 修复个人交易历史被其他玩家的交易挤空的问题：界面改查最近 14 天 CSV 账本（原只查全服共享的 200 条内存记录），每人最多显示 500 条
- 修复重置筛选后「我的」按钮不刷新的问题：精灵市场与管理员面板的精灵列表点「重置」后按钮仍显示「仅我的」状态
- 修复求购单交付精灵界面图标被切一角的问题：精灵图标待机动画改造时把该界面已放宽的裁剪区域覆盖回了旧值
- 修复求购特殊形态精灵时列表行图标显示默认形态的问题
- 修复自动备份模组备份期间误报「市场数据保存失败」红字：备份模组会临时暂停服务器保存（savingDisabled），此期间交易的强制落盘会被系统整体跳过导致验证误报——现在检测到保存被暂停时自动推迟落盘，备份结束后补写，不再误报
- 修复价格显示缺少千分位的问题：管理员面板的精灵列表悬停、管理员面板的物品下架确认、待领取物品悬停、价格限制列表与悬停、求购单发布冻结提示、物品市场购买总价，以及聊天消息里的金额（拍卖播报与加价提示、卡费用不足、成交与退款通知、贷款、限价提示等）现在统一显示千分位
- 修复拍卖出价与管理员面板的拍卖详情弹窗里携带物行不显示物品图标、标签还是灰色的问题（现与其他界面一致：白色标签 + 物品图标）
- 修复拍卖出价余额不足时金币音效与失败音效同时播放的问题：现在只播放失败音效，并在弹窗内红字提示余额不足（出价金额本身不合法时同样只有失败音效）
- 修复拍卖聊天播报的悬浮详情里起拍价与最低加价缺少冒号、最低加价金额没有金色与货币单位的问题
- 修复价格限制界面的价格缺少货币单位的问题：列表行与悬停窗的范围/上下限价格现在带单位（虚拟货币显示 ₽，物品货币显示物品名）
- 修复拍卖场「物品」「我的」tab 的搜索框提示文字不随 tab 更新：从拍卖上架界面返回或窗口缩放后会退回「宝可梦名称...」，现始终显示当前 tab 的提示；物品搜索提示统一为「搜索物品...」（拍卖场、黑名单、价格限制一致）
- 修复拍卖场与管理员面板的拍卖场的物品行长名称挤掉数量的问题：物品名过长时「×N」被一起截断，玩家看不到卖多少个——现在数量始终完整显示，名字自己截断
- **物品扣减改为按实际扣除量校验**：上架 / 拍卖 / 交付求购时此前按「计划扣除量」计数——若外部模组在数据层拦截了物品扣减，理论上会出现「挂单已入库但物品仍在背包」的虚增；现在按实际扣除量计，扣不掉就回滚（理论边界，未实际发生）
- **物品图标不显示耐久**：各处列表与详情弹窗里的物品此前都不显示耐久条，玩家看不出快报废的工具/装备、可能花原价买下；现在受损物品带原版耐久条，悬停窗还多一行「耐久：X / Y」（不必按 Shift）
- **交付求购单弹窗的买家留言被「选择形态」按钮盖住**：买家写了「耐久不限」这类要求时，卖家在交付弹窗里看不到（留言文字被按钮压住）；现在有留言时下方内容整体下移一行，留言完整可见
- **拍卖上架界面的最低加价提示不显示服务器默认值**：输入框占位符一直停在「留空=默认」，看不到具体数额——打开上架界面时就已经取回配置，但输入框要等点开上架窗口才创建，两步错开导致更新被跳过（窗口缩放后重建时才会碰巧显示对）；现在无论先后都能正确显示
- **市场关闭期间仍可新增借贷**：市场总开关（紧急停市）此前只拦买卖/拍卖/求购，金融侧的借款没被拦——停市后玩家照样能从喵喵银行借钱（且不限 OP，所有人都能借）。现在停市期间借款会被拒绝、喵喵银行内「应急贷款」按钮同步置灰；**存款、取款、还款与卡片申请不受影响**（那些都不会让钱流出准备金池）
- **市场关闭期间的界面拦截不到位**：市场关闭时精灵市场/物品市场/拍卖场/求购单四个入口看起来仍可点，点下去才知道关了；设置（玩家自己客户端的观感开关）与交易历史（只读账本）反而被一起拦死；喵喵银行里的「喵喵的帮助」置灰了却还点得进借款界面，玩家填完金额、点确认才被服务端拒绝。现在四个交易入口直接置灰（仍可点击，给音效与「市场功能已关闭」提示），设置与交易历史关市期间照常可用，「喵喵的帮助」置灰后点击只给提示、不再进入界面
- **管理操作的保存/添加/删除没有任何反馈**：保存服务器配置、保存权限、增删黑名单与价格限制、收回卡片、撤销坏账这些按钮点下去是完全静默的（只有一声很轻的按钮点击音），玩家分不清操作到底有没有生效；现在服务端处理完会给操作者回一声结果音——成功一声，被拒（物种解析失败、价格非法、该玩家不是持卡人等）也是一声失败音，不再只靠聊天栏红字；这批结果音与交易成功/失败音同响度

## 1.0.1（已发布）

### 改动

- **适配 Cobblemon 1.8，不再支持 1.7**：自 1.0.1 起及后续所有版本均要求 Cobblemon 1.8 以上。原因：1.8 的界面精灵渲染函数（drawProfilePokemon）签名发生破坏性变更（boolean → ProfileTransformType + 新增参数）；1.7.x 玩家请继续使用 1.0.0 版

## 1.0.0（已发布）

### 新功能

- 设置新增「待领取装不下掉落」开关（默认关）：开启后，领取待领取物品时背包放不下的部分会掉在地上（可能消失或被他人捡走，风险自负）；关闭时保持原样留在待领取，下次再领
- **管理员「所有求购」界面**：管理员面板新增入口，可查看全部求购单并强制下架——冻结金退还给买家，待确认交付的货物退回卖家，买卖双方都会收到通知（离线则上线补发）；面板按钮重新排列为两列布局
- **支持 Cobblemon Economy 货币**：Fabric 平台新增货币模式（现共四种：Cobblemon Economy / CobbleDollars / Impactor / 物品）——装了 Cobblemon Economy 的服务器，市场直接走其货币 API（其内置桥接可路由到 CobbleDollars/Impactor 后端，服主配置 main_currency 后市场与 CobbleDollars 商人共享同一余额；不装 Cobblemon Economy 也可单独直连 Impactor）。货币优先级 Cobblemon Economy → CobbleDollars → Impactor → 物品；全新安装自动探测，旧配置升级行为不变；新开关 `currency.cobblemonEconomy`，可选 `currency.cobecoCurrency`（POKE 默认/PCO）在 PokeDollars 与 PokeCoins 之间切换结算货币；价格单位统一为 ₽（PokeDollars/CobbleDollars 模式，行内与弹窗一致）
- **原生 NeoForge 支持**：新增 NeoForge 版模组（cobblemarket-neoforge-1.0.0.jar），与 Fabric 版功能一致、存档互通；依赖 Kotlin for Forge 与 Cobblemon（NeoForge 版），无需安装 Architectury API；Cobblemon Economy 无 NeoForge 版，该平台货币自动降级 CobbleDollars / Impactor / 物品（共三种）
- **容器内容校验**：物品黑名单、价格限制、蛋交易开关对容器内物品同样生效——上架/拍卖/求购单交付时递归检查容器内容（潜影箱等原版容器，模组容器不做判定），防止把受限物品装进容器绕过治理
- **物品求购交付可选形态**：背包中同一物品存在多种组件形态（如内容不同的潜影箱）时，交付前先选择要交付哪一种——选择列表显示图标与数量、悬停可见容器内容，交付弹窗内可随时更换；单形态交付流程不变
- **大木博士与知识点气泡**：入口界面新增常驻大木博士立绘，头顶聊天气泡随机展示宝可梦冷知识（内置 498 条中英双语，可自行增删替换）；每次进入入口界面随机换一条，点击大木博士可主动切换
- **配置热重载**：新增 `/market reload` 命令（OP）——费率、上限、时长、开关等配置改文件后即时生效，无需重启；市场总开关随重载变化时全员同步；货币配置需重启生效（重载时若检测到变更会提示）
- **服务器配置可视化编辑**：入口界面市场总开关左侧新增「服务器配置」按钮（仅 OP 可见）——费率、上限、时长、开关等 15 项配置可直接在游戏内编辑（数字输入框失焦保存、开关点击即生效），无需改配置文件；货币配置与拍卖时长列表仍需编辑配置文件
- **Impactor 直连支持**：不装 Cobblemon Economy 也能用 Impactor 货币——新增 `currency.impactor` 开关（双平台可用，NeoForge 服主从此有了虚拟货币直连路径），市场直接读写 Impactor 的 EconomyService API；货币优先级 Cobblemon Economy → CobbleDollars → Impactor → 物品；Impactor 不参与全新安装自动探测（它常被其它模组当作基础依赖安装，自动开启会静默切换货币），服主显式开启；配置注释补明 coblemonEconomy 仅 Fabric 平台生效
- **游戏内余额 HUD**：左上角显示市场余额（金色金额 + 深色圆角背景框，所有界面可见——竞价/购买弹窗里也能看到剩余余额）；物品货币模式本地实时计数（丢/捡货币物品立即变）；虚拟货币交易后即时刷新 + 30 秒低频兜底；设置弹窗三态切换——一直显示（默认）/ 余额变动时显示 5 秒/关闭
- **拍卖全服播报**：上架与成交都会在聊天框全服播报——拍品名悬浮可见详情（精灵等级/IV/EV/性格/球种，物品附魔词条），点击拍品名直达该拍品的出价界面
- **求购单验收直达**：求购单收到新交付时，通知消息自带「查看待交付」按钮，点击直达确认交付界面；离线期间的通知同样可点击
- **精灵图标动画**：所有界面的精灵图标默认播放 Cobblemon 内置待机动画，设置新增两态开关可切回完全静态
- **物品附魔词条可见**：拍卖与求购单交付的物品完整展示附魔等词条——列表悬停、出价弹窗、播报详情、验收弹窗四处一致
- **交易账本覆盖拍卖与求购单**：拍卖（上架/成交/流拍/强制下架）与求购单（发布/成交/关闭/过期/强制下架）全部写入交易历史 CSV，详情列新增附魔摘要，可精确复刻货物

### 改动

- 蛋交易开关从管理员面板迁移到「服务器配置」界面——入口界面市场总开关旁新增入口（仅 OP），编辑后点保存生效；开启蛋交易仍有二次确认弹窗（3 秒冷静期）；管理员面板返回按钮居中
- 拍卖出价低于当前价+最低加价时：按钮下方红字提示并播放失败音效（原先静默无反馈，且误播金币音效）
- 数量输入上限 3 位 → 4 位：物品上架数量、物品购买数量、拍卖物品数量最多可输入 9999
- 封禁管理、黑名单、价格限制界面新增的条目显示在最上面（最新的在前），方便管理
- 拍卖场与求购单列表同样最新的在最上面（实时新单也插在顶部）
- 交易历史 CSV 新增「详情」列：精灵完整数值（等级/闪光/IV/特训/性格/特性/性别/球种/携带物/形态）与物品 NBT 文本化，补偿对账时可按记录精确复刻
- 价格单位与货币名全模式统一：虚拟货币（Cobblemon Economy POKE/PCO、CobbleDollars）在行内、弹窗、悬停与聊天消息中一律只显示 ₽，不再显示 PCo/PokeDollars/PokeCoins 等货币名；物品货币仍显示物品名
- 入口界面与管理员面板背景重绘放大，多张贴图升级，两界面切换背景不再跳动；按钮布局不变
- 入口界面布局微调：标题加粗上移、行距收紧、分割线与市场关闭横幅位置优化
- 管理员面板标题与入口界面对齐（金色加粗）、按钮布局优化，未装 Cobbreeding 时返回按钮居中
- 分页布局改版（精灵/物品市场、管理员面板的列表）：列表下方新增对称分割线，上一页/下一页按钮不再遮住底部边框
- 修复三段背景界面最后一片贴图遮挡底部圆角的问题（全部 15 个界面）
- 求购单与拍卖界面面板贴图整合升级：共用贴图、删除重复资源
- 求购单入口按钮图标高清化，与市场总开关同等清晰度
- 交易历史界面（个人与全部历史视图）按钮行与记录列表之间新增分割线
- 悬停面板与确认弹窗新增「球种」文字说明（自定义球也能一眼认出）

### 修复

- **弹窗打开时下层物品图标与部分精灵模型图标刺穿遮罩**：市场/拍卖/管理员面板等既有界面均有此问题；物品类图标因渲染层限制在弹窗打开时隐藏，精灵 3D 图标用颜色参数压暗
- **市场类界面打开后持续掉帧**：挂单再多界面也能保持流畅
- **物品黑名单新增条目仍显示在列表最下面**（发送端漏加倒序）；黑名单/封禁/价格限制重复添加同一条目时不再停在旧位置，统一移到列表最前
- **求购单创建弹窗的校验错误提示被弹窗遮罩压暗看不见**（改到遮罩之上渲染）
- **拍卖结算/成交通知与精灵上架手续费的价格×费率 Int 溢出**（价格超过约 2.1 亿时环绕为负值或 0，导致手续费逃逸、卖家凭空多收或极端情形数据损坏）——改为 Long 运算
- **求购单交付被接受后的卖家通知显示错乱**：消息模板 5 个占位符只传 4 个参数且金额/货币顺序颠倒，到账金额、手续费与货币名混排并残留 %s
- **精灵选择界面搜索后首行 3D 图标固定显示队伍第一只精灵**（求购单交付与拍卖创建两处同源）：过滤缓存的内部序号被当作原列表索引查图标缓存，改为过滤时保留原始索引
- **玩家退出时可能误报「CobbleMarket 状态保存失败」**：NeoForge 端持久状态为异步写盘，保存后立即校验会误判失败；改为延迟校验，且无未落盘变更时不再触发保存
- **购买成功提示的金额显示为绿色而非金色**：消息模板金额占位符 %d 会吞掉 Text 颜色格式，改 %s 传入金色金额文本
- **市场类界面快速连点翻页后按钮永久变灰、内容停在旧页**：翻页点击改为「目标页码合并」——点击立即更新页码（每点必有反馈），请求按服务端节流窗口排队补发（精灵市场 250ms、物品市场/待领取 500ms），连点只发最终目标页的请求；另加 1 秒响应超时兜底，请求被静默丢弃也不会再锁死翻页按钮（精灵/物品市场与两个待领取界面；管理员面板的两个界面同步加超时兜底）
- **附属模组的自定义精灵球不显示球图标与球种名**（此前按 Cobblemon 命名空间解析导致空白）
- **市场强制下架的卖家通知**：精灵/物品被管理员强制下架时，卖家收到红色专属通知（与拍卖/求购一致）

## 1.0.0-beta.6（已发布）

### 新功能：求购单系统

- 玩家可发布「求购单」：精灵（固定 1 只）或物品（任意件数），附单价区间，全服可见
- 精灵求购支持：物种（留空=任意精灵）、闪光三态、特训三态、六项 IV 精确要求、形态、特性、性格
- 资金模式：发布时冻结「最高单价 × 需求数量」，成交按实际价结算并退还差价；关闭或过期自动退还剩余冻结金
- **买家确认制**：卖家交付先进入「待确认」状态（货物悬空、资金不结算、NBT 持久化重启不丢），买家查看完整货物信息后决定接受（结算成交）或拒绝（货物退回卖家、订单继续）；拒绝时可填写原因留言，卖家聊天框收到「您交付给 XX 的 XX 已被退回。留言：xxx」
- 订单有待确认交付时锁定新交付（防超量），买家不确认则订单过期时自动退回
- 多个卖家可对同一物品单部分交付（按件成交），凑满件数自动关闭
- 卖家交付：精灵经上架选择界面（交付模式）挑选并实时显示匹配/不匹配预核对；物品直接输入件数与单价；服务端二次核对（封禁/黑名单/蛋开关/价格限制——精灵按实际交付精灵匹配限价规则的形态/V 数/闪光/特训维度，物品按单价）后提交
- 求购单支持买家备注（额外需求，选填），行内不显示、悬停面板分割线区块与交付弹窗中展示
- 成交货物进买家待领取，成交记录进交易历史；中介费独立配置 `buyOrderFeePercent`（默认 5%），求购单有效期 `buyOrderExpiryDays`（默认 3 天），每人同时进行的求购单数量上限 `maxBuyOrdersPerPlayer`（默认 5，0=不限制；精灵与物品合计）
- 「我的求购」页签 可随时关闭求购单，冻结金立即退还
- 求购单列表支持搜索（物种/物品/买家名，本地即时过滤）

### 新功能：离线通知

- 玩家不在线时的交易通知（挂单卖出、被超价、拍卖成交/流拍、求购单交付/接受/拒绝/过期等）自动入队，上线后补发并按玩家语言渲染；每人最多保留 10 条

### 新功能：市场/拍卖/上架选择界面筛选改版

- 精灵市场、拍卖场、选择要上架的精灵（含交付模式）：属性筛选由循环切换改为展开列表（属性名按对应属性色显示），新增特性、性格展开选择（搜索框搜出物种后特性按钮列出该物种特性；性格按生效性格匹配，不分薄荷）
- 性别筛选改为公母图标按钮（♂♀ 双图标 = 不限，循环切换仅公/仅母）；选择要上架的精灵（含交付模式）同步补上公母按钮

### 新功能：交易体验增强

- 性格薄荷兼容：使用性格薄荷的精灵显示「斜体原生性格（生效性格薄荷）」，如 *胆小*（大胆薄荷）；未用薄荷显示正常；性格筛选与求购单匹配按生效性格判定
- 所有显示精灵详细信息的界面（市场/拍卖/管理/待领取/确认弹窗）名字行新增公母图标（♂蓝/♀红，与文字底部对齐）
- 获得精灵庆祝动画：**买到精灵、拍到精灵、求购单接受交付**时，获得者屏幕播放该精灵的弹跳球动画（拍卖场景与落槌铃声同步），任何界面都能看到；同一批获得多只时依次播完不互相覆盖；**开关分两层**：服主可用配置 `celebrationAnimationEnabled`（默认开启）全局关闭，玩家可在市场入口右下角的**设置**里按场景分别开关——「市场直购」与「拍卖/求购」各一个
- 封禁管理界面玩家名输入支持联想：候选覆盖该存档所有登录过的玩家（含离线，来自 usercache），输入前缀即可点选，杜绝输错名字
- **市场总开关**：服主发现漏洞等紧急情况可整体关闭市场——入口界面底部居中的开关按钮（仅 OP 可见，双态图标，与求购单/设置按钮同尺寸）或游戏内 `/market off`（`/market on` 恢复），配置 `marketEnabled`（默认开启）；**按钮停市前有二次确认弹窗**（3 秒冷静期 + 红白双色警告文字，恢复则立即生效不弹窗）；关闭后所有买卖/拍卖/求购操作被拦截并提示「市场功能已关闭」，玩家入口界面实时收到状态（登录补发 + 切换全员广播）并显示红字横幅；**关市期间玩家无法进入市场界面，待领取、领余额、取消挂单需等市场恢复后再操作**（资产不会丢失，恢复后一切照旧）；**OP 保留管理能力**（关市期间仍可进管理员面板执行下架/黑名单/价格限制等清理操作）

### 改动

- 配置文件升级友好：旧版本配置加载时自动补写新增字段（带默认值），服主升级模组后无需删除配置文件即可编辑新设置
- 拍卖上架界面的时长按钮改为读取服务器真实配置：服主可自定义拍卖时长与档位数量（几个档位、每个多长都随意），玩家看到的档位与实际结算一致
- 入口界面底部图标按钮：**左下角求购单**（点击进入求购单界面）、**右下角设置**（点击打开设置弹窗，目前含「市场直购动画」「拍卖/求购动画」两个开关，后续新增的客户端个人设置统一放这里）、底部居中为**市场总开关**（仅 OP 可见，双态图标）；三个小按钮上方有分割线，行 1/2 间距收紧、与底部按钮留出呼吸空间
- 弹窗观感统一：打开弹窗时下层列表内容保留、被半透明遮罩压暗（精灵 3D 图标同步压暗），弹窗背景加不透明衬底保证弹窗区域干净
- 行内价格显示优化：求购单价格区间与市场网格同款缩写（1 万以上用 k/M/B，悬停显示完整值）；余额与待领余额达 10 亿用 B 单位（以下保持完整千分位）
- 货币单位统一：弹窗/悬停面板里价格与货币单位统一显示为货币名（CobbleDollars 模式显示 PokéDollars、物品模式显示物品中文名）并统一用蓝色（与行内蓝色钻石符号一致）；行内继续用 ◆ 符号不占地方
- 所有界面中随属性变色的精灵名统一加阴影（暗色属性在灰色行背景上清晰可读）
- 精灵市场页码按钮预留空间优化：与「所有已上架精灵」界面一致，部分窗口高度下多显示一行列表

### 修复

- **服务器异常关闭（直接杀进程/崩溃）导致交易数据丢失**：上架的精灵/物品可能凭空消失——现在每次交易后数秒内强制写盘、玩家退出时立即保存市场数据，不再依赖自动保存周期；保存失败时会给在线 OP 发红字告警

## 1.0.0-beta.5（已发布）

### 新功能：支持 Cobblemon Utility+ 模组（极限特训）

- 极限特训后的个体值正确显示：市场/拍卖场/上架/待领取等界面显示「真实值（特训值）」格式（如 12（31）），与队伍详情界面一致
- 新增特训筛选（三态循环：不限/不含特训/仅特训）：精灵市场、管理员「所有已上架精灵」、拍卖场精灵页、上架与拍卖上架选择界面均可按特训状态筛选精灵
- IV 判定按有效值匹配：特训到 31 与原生 31 等效（搜索筛选、价格限制 V 数、黑名单 IV 匹配均按有效值判定）
- 精灵黑名单与精灵价格限制新增「特训」维度：规则可设为「不含特训」（只约束无特训的精灵，防止特训 6V 绕过按真实个体值判定的规则）；旧规则按「不限」兼容，行为不变；名单界面新增两态筛选按钮与悬停显示

### 改动

- 精灵黑名单与物品黑名单合并为一个「黑名单」界面：精灵/物品双页签 切换（与价格限制界面同款布局），原两个入口按钮合并为一个
- 精灵黑名单支持编辑规则：行内新增「编辑」按钮，弹窗预填全部字段（物种/个体值/形态/闪光/特训），保存替换原规则
- 精灵价格限制支持按形态区分：规则可限定全部形态/默认形态/指定形态，上架与拍卖起拍价校验均按形态匹配；行信息与悬停窗口显示形态
- 拍卖场 页签调整为 我的/精灵/物品 顺序，规则按钮并入页签行（四个按钮整体居中）

### 修复

- **物品黑名单弹窗的「全部解封」按钮**：弹窗打开时仍可点击，数据到达后按钮可见性也不刷新
- **精灵黑名单行信息过长时与删除按钮重叠**：现在以省略号截断（完整信息仍可在悬停窗口查看）
- **价格限制编辑条目后旧条目残留**：修改精灵（物种/V 数/闪光/形态）或物品后保存，现在正确替换原条目
- **精灵携带拉黑物品可绕过黑名单上架**：现在无法上架市场/拍卖场；携带物的价格限制并入总价校验：下限与精灵下限相加，上限在双方都设限时相加

## 1.0.0-beta.4（已发布）

### 新功能：拍卖行

- **拍卖场**：精灵/物品/我的三个页签，实时倒计时（最后 3 分钟显示秒级）、卖家头像、完整行信息（球种/属性色名/闪光星/性别/持有物）
- **上架拍卖**：精灵（搜索/IV/闪光/类型筛选与普通上架一致）+ 物品（背包扫描）双页签；起拍价受价格限制校验（物品按单价×数量换算总价区间）；最低加价可留空（用服务器默认）；时长档位；每人同时最多 3 个拍卖（精灵+物品合计，可配置）
- **竞价**：出价立即扣款；被超价自动退回待收款（黄字通知）；自己连续加价只补差价；不能给自己的拍卖出价；加价幅度校验
- **反狙击**：结束前 120 秒（可配置）内出价将结束时间重置为该秒数；结束后出价一律拒绝
- **结算**：到期自动结算（无需打开拍卖场，服务端每秒检查）；成交后赢家物品进待领取、卖家收成交价并扣手续费（可配置 0~100%）；无人出价则流拍退回卖家；结束后拍卖记录自动清理；卖家/赢家收到聊天通知
- **拍卖音效**：出价确认金币声；结束前 10 秒/6 秒/3 秒三声渐强锤声（配合行内锤子图标敲击动画）；成交落槌 + 铃声。音效只发给卖家与出价参与者，围观者不打扰
- **规则按钮**：拍卖场顶部悬停显示完整规则（标题金色/正文白色/重点红色/项间分割线，中英双语）
- **OP 强制下架**：管理员面板新增「所有拍卖」页（搜索/完整行信息/悬停/两列确认弹窗与拍卖场一致），点击任意拍卖可强制下架（物品退卖家待领取、当前出价者全额退款进待收款、全服同步移除）

### 新功能：精灵蛋交易（Cobbreeding 兼容）

——精灵蛋可上架市场与拍卖场：此前蛋会因孵化计时数据（timer/second 组件）每秒变化、上架校验无法匹配同一物品而失败，现已支持；不同的蛋严格区分、不会拿错

- 挂单里的蛋不会孵化，买家拿到后孵化进度与上架时看到的一致
- 上架界面实时显示孵化时间（与背包界面一致），孵化后条目自动消失
- 蛋交易开关：默认关闭；管理员面板一键开关，开启需二次确认（3 秒冷静期 + 红色风险提示：蛋绕过精灵黑名单、加密关闭时可被提前筛选）；关闭时市场与拍卖场上架、购买、出价蛋均被拒绝（治理即时生效，存量蛋挂单同样拦截）
- 与物品黑名单联动：黑名单优先级高于开关（可精细禁止某类蛋），配套批量拉黑/解封；拉黑后存量蛋挂单同样不可交易

### 改动

- 所有界面的价格显示统一：`金额 ◆` + 货币蓝（行内/悬停/弹窗/历史/待领取）
- 余额全局显示：入口/市场/物品市场/拍卖场/上架界面顶部显示实时余额（交易后自动刷新），待收款保持绿色
- 「过期返还」界面更名为「待领取」，行信息与悬停向精灵市场看齐（球种/性别/持有物/属性色）
- 物品市场购买库存不足时提示剩余数量（并发抢购场景）
- 拍卖成交写入交易历史（游戏内历史界面 + 本地中英 CSV），物种名正常翻译
- 调整列表行与悬停窗口的悬停、选中状态配色（row_background.png 纹理）
- 英文文本中 Pokemon 统一为官方拼写 Pokémon（界面与配置注释）
- 入口面板各按钮配图标（精灵市场/物品市场/交易历史/仅OP），与拍卖场按钮风格统一
- 管理员面板新增「所有拍卖」入口
- 物品黑名单支持批量拉黑（搜索匹配多个时一键全部添加，如蛋的全部属性变体）
- 配置注释完善：拍卖数量上限注明「精灵+物品合计」及人多服务器的性能建议
- 市场价格输入上限放宽到 9 位（与拍卖、价格限制一致）
- 物品市场与管理员面板「所有上架物品」每页物品上限从 30 提高到 84：窗口大时一页显示更多物品、减少翻页（窗口小时显示相应变少）
- 悬停面板与确认弹窗新增「球种：」文本行（自定义球一眼可辨）

### 修复

- **待领取界面物种名显示翻译 key 而非本地化名**（普通上架返还同样受影响）
- **英文模式下多处文字溢出**：待领取按钮文案缩短
- **服务器环境下货币名显示成物品英文原名**：现在按玩家自己的语言显示（界面与聊天消息都已覆盖）
- **物品购买/下架时可能误扣装备栏或副手上相同的物品**（极端情况下）：现在只动主背包
- **过期挂单延迟十几秒才下架**（期间仍可能被买走）：现在立即下架
- **黑名单与价格限制界面搜索后残留旧行的删除/编辑按钮**（点按钮或滚轮才消失）：搜索文字变化时立即按过滤结果重建行按钮
- **物品市场与管理员面板按名字搜索只过滤当前页**（目标在其他页时搜不到，需手动翻页）：搜索改为服务端全局过滤，与精灵市场一致，搜索后直接在第一页看到结果

## 1.0.0-beta.3（已发布）

### 新功能

- **价格限制**：管理员面板新增「价格限制」入口，精灵/物品双页签 管理——精灵规则支持四个维度（物种，留空 = 全部精灵；V 档，不限或恰好 0~6 个个体值 31；闪光筛选，不限/仅闪光/仅非闪光；最低价/最高价，可只填一侧，另一侧不限制）；物品规则为物品 + 最低价/最高价；上架校验命中多条规则时取最严交集，价格超界拒绝上架（只拦上架，不影响已上架挂单与购买）；规则支持添加、编辑（同组合重复添加自动覆盖）、删除，数据持久化并纳入存档自动备份链
- **精灵黑名单闪光筛选**：黑名单条目新增闪光筛选（不限/仅闪光/仅非闪光），上架与购买两处拦截同步生效；旧数据自动视为「不限」

### 改动

- 闪光标记全模组统一：金色 ★ = 闪光、白色 ☆ = 非闪光/关闭。市场筛选、上架选择、管理已上架、价格限制、黑名单五处按钮统一为纯符号（去掉文字）
- 列表行内的闪光标记由白色 ☆ 改为金色 ★（市场/管理/上架选择/退回精灵的列表行、悬停信息、确认弹窗信息行、市场图标角标）
- 价格限制与黑名单界面的行图标、添加对话框预览模型会按「仅闪光」规则渲染闪光配色
- 清理语言文件：删除重复的闪光按钮翻译键及无引用的 `sell.shiny`

## 1.0.0-beta.2（已发布）

### 修复

- **IV 筛选输入防抖**：快速输入「31」时列表会停留在个体值 3 的结果上；现在采用防抖，停止输入后统一请求
- **封禁信息按客户端语言显示**：修复英文服务端下中文玩家看到英文封禁提示的问题
- **分页按钮位置**：所有已上架物品界面的分页按钮不再紧贴面板边框

