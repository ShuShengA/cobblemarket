# 更新日志 / Changelog

## 1.0.0-beta.7（开发中，未发布）

### 新功能

- 设置新增「装不下的物品掉落」开关（默认关）：开启后，领取待领取物品时背包放不下的部分会掉在地上（可能消失或被他人捡走，风险自负）；关闭时保持原样留在待领取，下次再领

### 改动

- 数量输入上限 3 位 → 4 位：物品上架数量、物品购买数量、拍卖物品数量最多可输入 9999
- 封禁管理、黑名单、价格限制界面新增的条目显示在最上面（最新的在前），方便管理
- 拍卖场与求购单列表同样最新的在最上面（实时新单也插在顶部）

## 1.0.0-beta.6（开发中，未发布）

### 新功能：求购单系统

- 玩家可发布「求购单」：精灵（固定 1 只）或物品（任意件数），附单价区间，全服可见
- 精灵求购支持：物种（留空=任意精灵）、闪光三态、特训三态、六项 IV 精确要求、形态、特性、性格
- 资金模式：发布时冻结「最高单价 × 需求数量」，成交按实际价结算并退还差价；关闭或过期自动退还剩余冻结金
- **买家确认制**：卖家交付先进入「待确认」状态（货物悬空、资金不结算、NBT 持久化重启不丢），买家查看完整货物信息后决定接受（结算成交）或拒绝（货物退回卖家、订单继续）；拒绝时可填写原因留言，卖家聊天框收到「您交付给 XX 的 XX 已被退回。留言：xxx」
- 订单有待确认交付时锁定新交付（防超量），买家不确认则订单过期时自动退回
- 多个卖家可对同一物品单部分交付（按件成交），凑满件数自动关闭
- 卖家交付：精灵经上架选择界面（交付模式）挑选并实时显示匹配/不匹配预核对；物品直接输入件数与单价；服务端二次核对（封禁/黑名单/蛋开关/价格限制——精灵按实际交付精灵匹配限价规则的形态/V 数/闪光/特训维度，物品按单价）后提交
- 求购单支持买家备注（额外需求，选填），行内不显示、悬停面板分割线区块与交付弹窗中展示
- 成交货物进买家待领取，成交记录进交易历史；中介费独立配置 `buyOrderFeePercent`（默认 5%），求购单有效期 `buyOrderExpiryDays`（默认 3 天），每人同时进行的求购单数量上限 `maxBuyOrdersPerPlayer`（默认 5，0=不限制；精灵与物品合计）
- 「我的求购」tab 可随时关闭求购单，冻结金立即退还
- 求购单列表支持搜索（物种/物品/买家名，本地即时过滤）

### 新功能：离线通知

- 玩家不在线时的交易通知（挂单卖出、被超价、拍卖成交/流拍、求购单交付/接受/拒绝/过期等）自动入队，上线后补发并按玩家语言渲染；每人最多保留 10 条

### 新功能：市场/拍卖/上架选择界面筛选改版

- 精灵市场、拍卖场、选择要上架的精灵（含交付模式）：属性筛选由循环切换改为展开列表（属性名按对应属性色显示），新增特性、性格展开选择（搜索框搜出物种后特性按钮列出该物种特性；性格按生效性格匹配，不分薄荷）
- 性别筛选改为公母图标按钮（♂♀ 双图标 = 不限，循环切换仅公/仅母）；选择要上架的精灵（含交付模式）同步补上公母按钮，并在交付模式按当前语言自适应按钮宽度、重排第三行

### 新功能：交易体验增强

- 性格薄荷兼容：使用性格薄荷的精灵显示「斜体原生性格（生效性格薄荷）」，如 *胆小*（大胆薄荷）；未用薄荷显示正常；性格筛选与求购单匹配按生效性格判定
- 所有显示精灵详细信息的界面（市场/拍卖/管理/待领取/确认弹窗）名字行新增公母图标（♂蓝/♀红，与文字底部对齐）
- 获得精灵庆祝动画：**买到精灵、拍到精灵、求购单接受交付**时，获得者屏幕中央播放该精灵的**弹跳球动画**（从屏幕上方落到中央放大，再以最大形态弹跳 3 次高度递减，结尾淡出），拍卖场景与落槌铃声同步；通过客户端 Mixin 渲染在**所有界面之上**（打开任何界面都能看到，不在对应界面也能看到）；同一批获得多只时依次播完不互相覆盖；**开关分两层**：服主可用配置 `celebrationAnimationEnabled`（默认开启）全局关闭（关闭后服务端不下发动画包）；玩家可在市场入口右下角的**设置**里按场景分别开关——「市场直购」与「拍卖/求购」各一个（前者最高频，后两者低频合用一个；个人设置存 `config/cobblemarket-client.json`）。过期退回、取消挂单、管理端强制下架、从待领取领取均不播——那些精灵本来就是自己的
- 封禁管理界面玩家名输入支持联想：候选覆盖该存档所有登录过的玩家（含离线，来自 usercache），输入前缀即可点选，杜绝输错名字

### 改动

- 配置文件升级友好：旧版本配置加载时自动补写新增字段（带默认值），服主升级模组后无需删除配置文件即可编辑新设置
- 拍卖上架界面的时长按钮改为读取服务器真实配置：服主可自定义拍卖时长与档位数量（几个档位、每个多长都随意），玩家看到的档位与实际结算一致
- 入口界面底部图标按钮：**左下角求购单**（点击进入求购单界面）、**右下角设置**（点击打开设置弹窗，目前含「市场直购动画」「拍卖/求购动画」两个开关，后续新增的客户端个人设置统一放这里）、底部居中为**市场总开关**（仅 OP 可见，双态图标）；三个小按钮上方有分割线，行 1/2 间距收紧、与底部按钮留出呼吸空间
- 弹窗观感统一：打开弹窗时下层列表内容保留、被半透明遮罩压暗（精灵 3D 图标同步压暗），弹窗背景加不透明衬底保证弹窗区域干净
- 行内价格显示优化：求购单价格区间与市场网格同款缩写（1 万以上用 k/M/B，悬停显示完整值）；余额与待领余额达 10 亿用 B 单位（以下保持完整千分位）
- 货币单位统一：弹窗/悬停面板里价格与货币单位统一显示为货币名（CobbleDollars 模式显示 PokéDollars、物品模式显示物品中文名）并统一用蓝色（与行内蓝色钻石符号一致）；行内继续用 ◆ 符号不占地方
- **市场总开关**：服主发现漏洞等紧急情况可整体关闭市场——入口界面底部居中的开关按钮（仅 OP 可见，双态图标，与求购单/设置按钮同尺寸）或游戏内 `/market off`（`/market on` 恢复），配置 `marketEnabled`（默认开启）；**按钮停市前有二次确认弹窗**（3 秒冷静期 + 红白双色警告文字，恢复则立即生效不弹窗）；关闭后所有买卖/拍卖/求购操作被拦截并提示「市场功能已关闭」，玩家入口界面实时收到状态（登录补发 + 切换全员广播）并显示红字横幅；**取回自己资产不受影响**（待领取、领余额、取消挂单仍可用），及时止损不卡资产；**OP 保留管理能力**（关市期间仍可进管理面板执行下架/黑名单/价格限制等清理操作）
- tab 切换选中标记的黑色圆点改为无阴影绘制（保持正圆）
- 所有界面中随属性变色的精灵名统一加阴影（暗色属性在灰色行背景上清晰可读）
- 精灵市场页码按钮预留空间优化：与「所有已上架精灵」界面一致，部分窗口高度下多显示一行列表

### 修复

- 修复弹窗打开时下层物品图标（球种/携带物等，drawItem 渲染层）与部分精灵模型图标（发光层）刺穿遮罩的问题——市场/拍卖/管理端等既有界面均有此问题；物品类图标因渲染层限制在弹窗打开时隐藏，精灵 3D 图标用颜色参数压暗

## 1.0.0-beta.5（开发完成，未发布）

### 新功能：支持 Cobblemon Utility+ 模组（极限特训）

- 极限特训后的个体值正确显示：市场/拍卖场/上架/待领取等界面显示「真实值（特训值）」格式（如 12（31）），与队伍详情界面一致
- 新增特训筛选（三态循环：不限/不含特训/仅特训）：精灵市场、管理员「所有已上架精灵」、拍卖场精灵页、上架与拍卖上架选择界面均可按特训状态筛选精灵
- IV 判定按有效值匹配：特训到 31 与原生 31 等效（搜索筛选、价格限制 V 数、黑名单 IV 匹配均按有效值判定）
- 精灵黑名单与精灵价格限制新增「特训」维度：规则可设为「不含特训」（只约束无特训的精灵，防止特训 6V 绕过按真实个体值判定的规则）；旧规则按「不限」兼容，行为不变；名单界面新增两态筛选按钮与悬停显示

### 新功能：黑名单与价格限制增强

- 精灵黑名单与物品黑名单合并为一个「黑名单」界面：精灵/物品双 tab 切换（与价格限制界面同款布局），原两个入口按钮合并为一个
- 精灵黑名单支持编辑规则：行内新增「编辑」按钮，弹窗预填全部字段（物种/个体值/形态/闪光/特训），保存替换原规则
- 精灵价格限制支持按形态区分：规则可限定全部形态/默认形态/指定形态，上架与拍卖起拍价校验均按形态匹配；行信息与悬停窗口显示形态

### 改动

- 拍卖场 tab 调整为 我的/精灵/物品 顺序，规则按钮并入 tab 行（四个按钮整体居中）

### 修复

- 物品黑名单弹窗打开时「全部解封」按钮仍可点击、数据到达后该按钮可见性不刷新的问题
- 精灵黑名单行信息过长（如六项个体值全填）时以省略号截断，不再与右侧删除按钮重叠（完整信息仍可在悬停窗口查看）
- 价格限制编辑条目时若修改了精灵（物种/V 数/闪光/形态）或物品，保存后旧条目不再残留，正确替换原条目
- 精灵携带拉黑物品时无法上架市场/拍卖场（此前可绕过物品黑名单间接出售拉黑物品）；携带物的价格限制并入总价校验：下限与精灵下限相加，上限在双方都设限时相加

## 1.0.0-beta.4（已发布）

### 新功能：拍卖行

- **拍卖场**：精灵 / 物品 / 我的三个 tab，实时倒计时（最后 3 分钟显示秒级）、卖家头像、完整行信息（球种/属性色名/闪光星/性别/持有物）
- **上架拍卖**：精灵（搜索/IV/闪光/类型筛选与普通上架一致）+ 物品（背包扫描）双 tab；起拍价受价格限制校验（物品按单价×数量换算总价区间）；最低加价可留空（用服务器默认）；时长档位；每人同时最多 3 个拍卖（精灵+物品合计，可配置）
- **竞价**：出价立即扣款；被超价自动退回待收款（黄字通知）；自己连续加价只补差价；不能给自己的拍卖出价；加价幅度校验
- **反狙击**：结束前 120 秒（可配置）内出价将结束时间重置为该秒数；结束后出价一律拒绝
- **结算**：到期自动结算（无需打开拍卖场，服务端每秒检查）；成交后赢家物品进待领取、卖家收成交价并扣手续费（可配置 0~100%）；无人出价则流拍退回卖家；结束后拍卖记录自动清理；卖家/赢家收到聊天通知
- **拍卖音效**：出价确认金币声；结束前 10 秒/6 秒/3 秒三声渐强锤声（配合行内锤子图标敲击动画）；成交落槌 + 铃声。音效只发给卖家与出价参与者，围观者不打扰
- **规则按钮**：拍卖场顶部悬停显示完整规则（标题金色/正文白色/重点红色/项间分割线，中英双语）
- **OP 强制下架**：管理员面板新增「所有拍卖」页（搜索/完整行信息/悬停/两列确认弹窗与拍卖场一致），点击任意拍卖可强制下架（物品退卖家待领取、当前出价者全额退款进待收款、全服同步移除）
### 精灵蛋交易（Cobbreeding 兼容）

——精灵蛋可上架市场与拍卖场：此前蛋会因孵化计时数据（timer/second 组件）每秒变化、上架校验无法匹配同一物品而失败，现已支持；不同的蛋严格区分、不会拿错

- 挂单里的蛋不会孵化，买家拿到后孵化进度与上架时看到的一致
- 上架界面实时显示孵化时间（与背包界面一致），孵化后条目自动消失
- 蛋交易开关：默认关闭；管理员面板一键开关，开启需二次确认（3 秒冷静期 + 红色风险提示：蛋绕过精灵黑名单、加密关闭时可被提前筛选）；关闭时市场与拍卖场上架、购买、出价蛋均被拒绝（治理即时生效，存量蛋挂单同样拦截）
- 与物品黑名单联动：黑名单优先级高于开关（可精细禁止某类蛋），配套批量拉黑/解封；拉黑后存量蛋挂单同样不可交易

### 改动

- 所有界面的价格显示统一：`金额 ◆` + 货币蓝（行内/悬停/弹窗/历史/待领取）
- 余额全局显示：入口/市场/物品市场/拍卖场/上架界面顶部显示实时余额（交易后自动刷新），待收款保持绿色
- 「过期返还」界面更名为「待领取」，行信息与悬停向精灵市场看齐（球种/性别/持有物/属性色）
- 调整列表行与悬停窗口的悬停、选中状态配色（row_background.png 纹理）
- 英文文本中 Pokemon 统一为官方拼写 Pokémon（界面与配置注释）
- 入口面板各按钮配图标（精灵市场/物品市场/交易历史/仅OP），与拍卖场按钮风格统一
- 管理员面板新增「所有拍卖」入口
- 物品黑名单支持批量拉黑（搜索匹配多个时一键全部添加，如蛋的全部属性变体）
- 物品市场购买库存不足时提示剩余数量（并发抢购场景）
- 拍卖成交写入交易历史（游戏内历史界面 + 本地中英 CSV），物种名正常翻译
- 配置注释完善：拍卖数量上限注明「精灵+物品合计」及人多服务器的性能建议
- 市场价格输入上限放宽到 9 位（与拍卖、价格限制一致）
- 物品市场与管理端「所有上架物品」每页物品上限从 30 提高到 84：窗口大时一页显示更多物品、减少翻页（窗口小时显示相应变少）

### 修复

- 待领取界面物种名显示翻译 key 而非本地化名（普通上架返还同样受影响）
- 英文模式下多处文字溢出：待领取按钮文案缩短
- 服务器环境下货币名显示成物品英文原名：现在按玩家自己的语言显示（界面与聊天消息都已覆盖）
- 物品购买/下架时可能误扣玩家装备栏或副手上相同的物品（极端情况下）：已修复，现在只动主背包
- 过期挂单现在立即下架（原来会延迟十几秒，期间仍可能被买走）
- 黑名单与价格限制界面搜索后残留旧列表的删除/编辑按钮（点按钮或滚轮才消失）：搜索文字变化时立即按过滤结果重建行按钮
- 物品市场与管理端「所有上架精灵/物品」按名字搜索只过滤当前页（目标在其他页时搜不到，需手动翻页）：搜索改为服务端全局过滤，与精灵市场一致，搜索后直接在第一页看到结果

