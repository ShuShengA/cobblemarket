# CobbleMarket 开发约定

## 铁律：版本升级不得损坏旧存档

**每发一个新版本，必须保证旧版本玩家/服务器的数据不因升级而损坏。** 改动数据层时照下面做：

1. **加字段只增不改** —— 新增字段一律追加在末尾并给默认值，旧档反序列化时缺字段自动落默认；不改动已有字段的类型或语义
2. **新数据开新 NBT 键** —— 不把旧键的结构改成嵌套形式（旧档读不到新键即空值），因此**不需要写迁移分支**，迁移分支本身就是出错源
3. **读取容错** —— `contains()` 判断缺失字段；逐条 `try/catch` 跳过损坏条目并 `LOGGER.warn`，绝不整体抛异常
4. **枚举/字符串容错** —— `runCatching { valueOf(x) }.getOrDefault(默认)`，非法值不崩

改动数据层前先问三件事：

- ① 这是**新增**还是**改结构**？（改结构最危险，优先想办法避免）
- ② 旧档读到它会**落默认**还是**崩**？
- ③ 写盘后**旧版本还能读吗**？

**配套判据**：改存档结构 / 改 payload 属于「大版本」范畴（客户端服务端必须同步升级）；纯客户端渲染属于「小版本」。

## 铁律：能改的版本就是没发布的版本

**正在哪个版本分支上改代码，那个版本就一定没发布过** —— 发布了就不会再改它（要改只能发新版本，不可能同一个版本号发两次）。据此：

- 「要不要兼容旧配置 / 旧存档」→ **不用**，没有旧数据
- 「这个『已废弃』标注要不要写」→ **不用**，公众没见过旧版本，写了只会让人困惑
- 「这算不算历史包袱 / 要不要写迁移分支」→ **不算**，从未发布的版本没有包袱

只有当改动涉及**已发布版本**时，才谈兼容与迁移 —— 那时适用上面那条铁律。

## 约定：做完功能顺手记「官网同步清单」

每完成一个**面向玩家**的功能或改动，**往 `docs/website_sync_plan.md` 里对应版本的清单加一行**
（写清"官网哪一页要跟着动"）。官网同步是本项目的一部分 —— **模组动，网站就要动**。

**为什么**：如果等发布时才动官网，中间已经清过很多次上下文，细节回忆不起来、必然写漏。
这份清单跟着仓库走（不是记忆），不会丢，而且能直接指导生成网站页面。

## 参考文档

- **Cobblemon 大版本迁移**（官方移植到新 MC / 大重构时）：评估与预案见 `docs/cobblemon_migration_plan.md` —— 含耦合实测数据、风险排序（存档精灵 NBT > 语义变更 > 渲染层重写）、分批恢复顺序。**触发前不需要做任何准备**，别提前重构
